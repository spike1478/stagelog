version: '3.8'

services:
  stagelog:
    image: nginx:alpine
    container_name: stagelog
    ports:
      - "17492:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
